---
phase: 04-ship-it
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - .github/workflows/ci.yml
  - vercel.json
  - README.md
autonomous: true
requirements: [DEPL-01, DEPL-02, DEPL-03]

must_haves:
  truths:
    - "GitHub Actions CI runs lint, unit tests, and build on every push"
    - "Vercel deployment configuration exists with required environment variables documented"
    - "README documents setup instructions, architecture, key decisions, and tradeoffs"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions CI pipeline"
      min_lines: 30
    - path: "README.md"
      provides: "Project README with setup, architecture, decisions, and tradeoffs"
      min_lines: 80
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "package.json"
      via: "runs pnpm lint, pnpm test, pnpm build"
      pattern: "pnpm"
    - from: "README.md"
      to: ".planning/PROJECT.md"
      via: "documents architecture and decisions from planning docs"
      pattern: "Architecture"
---

<objective>
Set up CI pipeline, Vercel deployment configuration, and comprehensive README documentation.

Purpose: Final deliverable packaging — CI ensures quality gates, Vercel config enables deployment, and README communicates the project's architecture and decisions to reviewers.
Output: Working CI pipeline, deployment-ready config, and polished README.
</objective>

<execution_context>
@/Users/eduard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eduard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
Package manager: pnpm (pnpm-lock.yaml exists)
Linter: oxlint (script: "lint": "oxlint")
Test runner: vitest (script: "test": "vitest run") — added in plan 04-02
E2E: playwright (script: "test:e2e": "playwright test") — added in plan 04-02
Build: Next.js 16 ("build": "next build")
Node version: use 20.x for CI

Environment variables needed for deployment:
- NEXT_PUBLIC_SUPABASE_URL — Supabase project URL
- NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY — Supabase publishable (anon) key

Key decisions to document (from STATE.md accumulated context):
- shadcn/ui with base-nova style, custom properties referencing Pencil design
- Font loading via next/font/google (Fraunces, Inter, IBM Plex Mono)
- nuqs v2 for URL state management
- proxy.ts + middleware.ts split for Supabase auth
- useActionState for login form
- react-day-picker for date selection
- Server components for data fetching, client components only where interactivity needed
- ReviewForm via prop slot pattern
- session_replication_role=replica for seeding
- Fixed UUIDs for seed data cross-references
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub Actions CI pipeline and Vercel deployment config</name>
  <files>
    .github/workflows/ci.yml
    vercel.json
  </files>
  <action>
1. **Create .github/workflows/ci.yml** — CI pipeline:
   ```yaml
   name: CI
   on:
     push:
       branches: [main]
     pull_request:
       branches: [main]
   jobs:
     ci:
       runs-on: ubuntu-latest
       steps:
         - uses: actions/checkout@v4
         - uses: pnpm/action-setup@v4
           with:
             version: 9
         - uses: actions/setup-node@v4
           with:
             node-version: 20
             cache: pnpm
         - run: pnpm install --frozen-lockfile
         - run: pnpm lint
         - run: pnpm test
         - run: pnpm build
           env:
             NEXT_PUBLIC_SUPABASE_URL: https://placeholder.supabase.co
             NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY: placeholder-key-for-build
   ```
   Notes:
   - The build step needs placeholder env vars because the client env validation (Zod) runs at build time
   - Lint uses oxlint (fast, no config needed)
   - Tests are unit tests only (no E2E in CI — would need Supabase)
   - E2E tests are documented as manual step in README

2. **Create vercel.json** — Minimal Vercel configuration:
   ```json
   {
     "$schema": "https://openapi.vercel.sh/vercel.json",
     "framework": "nextjs"
   }
   ```
   The actual env vars (NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY) are set in the Vercel dashboard, not in vercel.json.
  </action>
  <verify>
    <automated>cd /Users/eduard/projects/03-demos/lateral-challenge && cat .github/workflows/ci.yml | head -30 && echo "---" && cat vercel.json</automated>
  </verify>
  <done>
    - .github/workflows/ci.yml exists with lint, test, build steps
    - CI uses pnpm with frozen lockfile
    - Build step includes placeholder env vars for Zod validation
    - vercel.json exists with framework: nextjs
  </done>
</task>

<task type="auto">
  <name>Task 2: Write comprehensive README with setup, architecture, decisions, and tradeoffs</name>
  <files>
    README.md
  </files>
  <action>
Replace the default create-next-app README with a comprehensive project README. Structure:

## Lateral Challenge

One-paragraph description: A vibe-first unique stays booking app built as an interview challenge. Users discover treehouses, cabins, glamping sites, and houseboats through a "Who's traveling?" + "What's the vibe?" flow, then book through a multi-step checkout.

## Screenshots

(Placeholder — note that screenshots can be added from the running app)

## Quick Start

Prerequisites:
- Node.js 20+
- pnpm 9+
- Supabase CLI (`brew install supabase/tap/supabase`)
- Docker (for local Supabase)

```bash
git clone <repo-url>
cd lateral-challenge
pnpm install

# Start local Supabase (Postgres + Auth)
supabase start

# Copy env vars from supabase start output
cp .env.example .env.local
# Fill in NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY

# Apply migrations and seed data
supabase db reset

# Generate TypeScript types
pnpm db:types

# Start dev server
pnpm dev
```

Open http://localhost:3000

## Architecture

### Tech Stack
- **Framework:** Next.js 16 (App Router, React 19, Server Components)
- **Database:** Supabase (Postgres + Auth + RLS)
- **Styling:** Tailwind CSS v4 + shadcn/ui (base-nova style)
- **State:** nuqs v2 for URL params, React server components for data
- **Testing:** Vitest (unit) + Playwright (E2E)
- **CI/CD:** GitHub Actions + Vercel

### Project Structure
```
app/
  (main)/             # Shared layout with header
    page.tsx           # Discovery — vibe picker + stays grid
    stays/[slug]/      # Stay detail, checkout, confirmation
    profile/           # My bookings
  auth/               # Login + callback (no header)
components/           # Shared components (UI + domain)
lib/
  actions/            # Server actions (stays, bookings, reviews, auth)
  supabase/           # Client helpers, proxy, types
  utils/              # Price calculation, date utilities
supabase/
  migrations/         # SQL migrations
  seed.sql            # Seed data (15 stays, 5 collections, 20 reviews)
```

### Data Flow
- Server Components fetch data via server actions (no client-side API calls)
- Client Components only where interactivity needed (forms, pickers, dropdowns)
- URL state managed by nuqs — filter params (?type=solo&vibe=adventure) are shareable and SSR-compatible
- Auth session managed by Supabase proxy middleware (refreshes tokens on every request)

## Key Decisions

| Decision | Rationale |
|----------|-----------|
| Vibe-first discovery over search box | Matches the product vision — users explore by mood, not destination |
| Server Components for data fetching | No client-side loading waterfalls, SEO-friendly, simpler data flow |
| nuqs for URL state | Shareable filter URLs, SSR-compatible, type-safe search params |
| proxy.ts + middleware.ts split | Keeps auth logic in its own module, middleware stays thin |
| Magic link auth (no password) | Simpler UX for demo, no password reset flow needed |
| react-day-picker for dates | Better UX than native date inputs, range selection built-in |
| shadcn/ui base-nova style | Matches Pencil design system, warm organic aesthetic |
| Fixed UUIDs in seed data | Enables reliable cross-references between stays, collections, reviews |
| session_replication_role=replica for seeding | Bypasses RLS during seed without needing auth.users records |
| oxlint over ESLint | 50-100x faster, sufficient for project scope |

## Tradeoffs

| What | Alternative considered | Why this choice |
|------|----------------------|----------------|
| Mock payment | Stripe integration | Demo scope — document Stripe plan instead |
| Hardcoded Unsplash images | Image upload | Avoids storage complexity, consistent seed data |
| No content moderation | Review approval workflow | Reviews go live immediately, acceptable for demo |
| No map view | Mapbox/Leaflet integration | Deferred to v2, core flow works without it |
| No i18n | next-intl | Out of scope for timebox |
| Vitest over Jest | Jest | Faster, native ESM support, better Next.js compatibility |

## Scripts

| Script | Description |
|--------|-------------|
| `pnpm dev` | Start development server |
| `pnpm build` | Production build |
| `pnpm lint` | Run oxlint |
| `pnpm test` | Run unit tests (Vitest) |
| `pnpm test:e2e` | Run E2E tests (Playwright, requires dev server) |
| `pnpm db:types` | Regenerate Supabase TypeScript types |

## Environment Variables

| Variable | Description |
|----------|-------------|
| `NEXT_PUBLIC_SUPABASE_URL` | Supabase project URL |
| `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY` | Supabase publishable (anon) key |

For local development, get these from `supabase start` output.

## Testing

**Unit tests:** Cover price calculation (calculateNights, calculateTotal, formatPrice) and date utilities. Run with `pnpm test`.

**E2E tests:** Cover the browsing-to-checkout flow. Requires a running dev server and local Supabase. Run with `pnpm test:e2e`.

## Deployment

1. Push to GitHub
2. Connect repo to Vercel
3. Set environment variables in Vercel dashboard
4. Deploy — Vercel auto-detects Next.js

CI runs lint + test + build on every push to main and on PRs.
  </action>
  <verify>
    <automated>cd /Users/eduard/projects/03-demos/lateral-challenge && wc -l README.md</automated>
  </verify>
  <done>
    - README has Quick Start with all prerequisites and setup steps
    - Architecture section covers tech stack, project structure, and data flow
    - Key Decisions table documents 10+ decisions with rationale
    - Tradeoffs table documents what was NOT built and why
    - Scripts table documents all available commands
    - Environment variables documented
    - Deployment instructions included
  </done>
</task>

</tasks>

<verification>
1. `.github/workflows/ci.yml` exists and is valid YAML with lint, test, build steps
2. `vercel.json` exists with framework: nextjs
3. README.md has sections: Quick Start, Architecture, Key Decisions, Tradeoffs, Scripts, Deployment
4. README.md is at least 80 lines
5. `npx tsc --noEmit` still passes
</verification>

<success_criteria>
- CI pipeline runs lint, unit tests, and build on push/PR
- Vercel deployment is configured
- README documents setup, architecture, 10+ key decisions, tradeoffs, and deployment
- A reviewer can clone the repo and follow README to run the app locally
</success_criteria>

<output>
After completion, create `.planning/phases/04-ship-it/04-03-SUMMARY.md`
</output>
