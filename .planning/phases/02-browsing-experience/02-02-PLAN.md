---
phase: 02-browsing-experience
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - components/stay-card.tsx
  - components/stays-grid.tsx
  - components/search-bar.tsx
  - components/sort-toggle.tsx
  - app/(main)/page.tsx
autonomous: true
requirements: [DISC-03, DISC-04, DISC-05, DISC-06, DISC-07, DISC-08]

must_haves:
  truths:
    - "User sees stay cards in a responsive grid: 1 column on mobile, 2 on tablet, 3 on desktop"
    - "Each stay card shows image, title, location, price/night, max guests, and type badge"
    - "User can type in a search bar to filter stays by title or location"
    - "User can toggle sort between price low-to-high and high-to-low"
    - "A loading skeleton grid appears while stays are being fetched"
    - "Stay cards on mobile show horizontal layout (image left, info right) per Pencil design"
  artifacts:
    - path: "components/stay-card.tsx"
      provides: "Individual stay card component with image, title, location, price, badge"
    - path: "components/stays-grid.tsx"
      provides: "Responsive grid wrapper for stay cards"
    - path: "components/search-bar.tsx"
      provides: "Text search input wired to nuqs search param"
    - path: "components/sort-toggle.tsx"
      provides: "Price sort toggle wired to nuqs sort param"
  key_links:
    - from: "components/search-bar.tsx"
      to: "lib/search-params.ts"
      via: "nuqs useQueryState"
      pattern: "useQueryState.*search"
    - from: "components/sort-toggle.tsx"
      to: "lib/search-params.ts"
      via: "nuqs useQueryState"
      pattern: "useQueryState.*sort"
    - from: "components/stay-card.tsx"
      to: "/stays/[id]"
      via: "Next.js Link"
      pattern: "Link.*href.*stays"
---

<objective>
Build the stay cards, responsive grid, search bar, and sort toggle to complete the discovery page. Stay cards match the Pencil design with image, title, location, price/night, and type badge. The grid is responsive (1/2/3 columns). Search and sort update URL state via nuqs.

Purpose: This completes the browsing experience — users can see, search, and sort stays after filtering by vibe.
Output: Fully functional discovery page with cards, search, sort, and responsive layout.
</objective>

<execution_context>
@/Users/eduard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eduard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-browsing-experience/02-01-SUMMARY.md

<interfaces>
<!-- From plan 02-01 (must exist before this plan runs) -->

From lib/search-params.ts:
```typescript
import { parseAsString, createSearchParamsCache } from 'nuqs/server';
export const searchParamsParsers = {
  type: parseAsString,
  vibe: parseAsString,
  search: parseAsString,
  sort: parseAsString,
};
export const searchParamsCache = createSearchParamsCache(searchParamsParsers);
```

From lib/actions/stays.ts:
```typescript
export type StayCard = Tables<'stays'>;
// StayCard has: id, title, slug, location, price_per_night, cleaning_fee, service_fee, max_guests, type, vibe, travel_type, amenities, images, description
export async function getStays(filters: {
  type?: string | null;
  vibe?: string | null;
  search?: string | null;
  sort?: string | null;
}): Promise<StayCard[]>;
```

From lib/supabase/types.ts — stays.Row:
- price_per_night: number (in cents)
- images: string[] (Unsplash URLs)
- type: 'treehouse' | 'cabin' | 'glamping' | 'houseboat' | 'yurt'
- travel_type: 'solo' | 'duo' | 'family' | 'group'
- vibe: 'adventure' | 'culture' | 'disconnect' | 'celebration'

Design tokens (from globals.css):
- Colors: bg-bg-page, bg-bg-card, bg-bg-muted, text-text-primary, text-text-body, text-text-secondary, text-warm-accent, text-accent, border-border
- Radii: rounded-[--radius-card] (20px), rounded-[--radius-badge] (16px), rounded-[--radius-pill] (999px)
- Fonts: font-heading, font-sans, font-mono
</interfaces>

<design_reference>
Pencil design screenshots analyzed — key patterns:

**Stay Card Desktop (Discovery/Home - node 8YqpV):**
- Vertical card layout: image on top (aspect ~4:3, rounded-[--radius-card] corners top), content below
- Image: fills card width, object-cover
- Below image: title (font-heading text-base font-semibold), location (text-text-secondary text-sm), price ("$185 / night" — dollar in font-mono font-semibold, "/ night" in text-text-secondary text-sm)
- Bottom row: type badge (e.g., "Treehouse") as pill — bg-bg-muted text-text-secondary text-xs rounded-[--radius-badge] px-3 py-1 — and max guests indicator (e.g., "4 guests" with Users icon)
- Card has bg-bg-card, rounded-[--radius-card], subtle shadow-sm or border-border-subtle
- Grid: 3 columns on desktop, gap-6

**Stay Card Mobile (Mobile Compact - node rF0Ok):**
- Horizontal layout: image on left (~120px square, rounded-[--radius-small]), info on right
- Info stacked vertically: title (font-semibold), location (text-sm text-text-secondary), price ("$245 / night")
- Type badge at bottom right
- Single column list with gap-4

**Price display:**
- Format: `$185 / night` where price = price_per_night / 100 (stored in cents)
- Price uses font-mono font-semibold, "/ night" in text-text-secondary regular weight

**Sort toggle (top right of grid section):**
- Text link style: "↕ Price: low → high" / "↕ Price: high → low"
- Positioned inline with the section title "Unique stays near you"
</design_reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StayCard component with desktop and mobile variants</name>
  <files>
    components/stay-card.tsx
    components/stays-grid.tsx
  </files>
  <action>
1. Create `components/stay-card.tsx` — Server component (no 'use client' needed):
- Accept props: `stay: StayCard` (the Tables<'stays'> type from lib/actions/stays.ts)
- Wrap entire card in `<Link href={/stays/${stay.slug || stay.id}}>` for navigation to detail page
- **Desktop variant** (hidden on mobile, shown md+):
  - Vertical card: bg-bg-card rounded-[--radius-card] overflow-hidden border border-border-subtle hover:shadow-md transition-shadow
  - Image: `<Image>` from next/image, src={stay.images[0]}, fill, className="object-cover", wrapped in `<div className="relative aspect-[4/3]">`
  - Content padding: p-4 flex flex-col gap-2
  - Title: `<h3 className="font-heading text-base font-semibold text-text-primary line-clamp-1">`
  - Location: `<p className="text-sm text-text-secondary">{stay.location}</p>`
  - Price row: `<div className="flex items-baseline gap-1"><span className="font-mono font-semibold text-text-primary">${(stay.price_per_night / 100).toFixed(0)}</span><span className="text-sm text-text-secondary">/ night</span></div>`
  - Bottom row: flex justify-between items-center. Left: type badge as `<span className="rounded-[--radius-badge] bg-bg-muted px-3 py-1 text-xs text-text-secondary capitalize">{stay.type}</span>`. Right: `<span className="flex items-center gap-1 text-xs text-text-secondary"><Users size={14} /> {stay.max_guests} guests</span>`

- **Mobile variant** (shown on mobile, hidden md+):
  - Horizontal card: flex flex-row gap-4, bg-bg-card rounded-[--radius-small] overflow-hidden border border-border-subtle p-3
  - Image: `<div className="relative h-24 w-24 flex-shrink-0 rounded-[--radius-small] overflow-hidden"><Image fill ... /></div>`
  - Info: flex flex-col justify-between flex-1 min-w-0
  - Title, location, price stacked. Badge bottom right.

2. Create `components/stays-grid.tsx` — Server component:
- Accept props: `stays: StayCard[]`
- If stays.length === 0, show empty state: centered icon (SearchX from lucide), "No stays found" heading, "Try adjusting your filters" body text
- Responsive grid: `<div className="grid grid-cols-1 gap-4 md:grid-cols-2 md:gap-6 lg:grid-cols-3">`
- Map over stays, render `<StayCard key={stay.id} stay={stay} />` for each
- The grid shows desktop cards on md+ and mobile cards below md (handled by StayCard's internal responsive classes)
  </action>
  <verify>
    <automated>cd /Users/eduard/projects/03-demos/lateral-challenge && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>
    - StayCard renders image, title, location, price/night, type badge, guest count
    - Desktop layout: vertical card with image on top
    - Mobile layout: horizontal card with image on left
    - StaysGrid renders responsive grid (1/2/3 columns)
    - Empty state shown when no stays match
    - Price displays correctly in dollars (converted from cents)
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Build search bar, sort toggle, wire everything into the discovery page</name>
  <files>
    components/search-bar.tsx
    components/sort-toggle.tsx
    app/(main)/page.tsx
  </files>
  <action>
1. Create `components/search-bar.tsx` — Client component:
- `'use client'`
- Import `useQueryState` from `nuqs` and `searchParamsParsers` from `lib/search-params`
- Render a search input: `<div className="relative">` with Search icon (lucide) positioned absolute left, input with pl-10
- Input styling: bg-bg-card border border-border rounded-[--radius-pill] px-4 py-2 text-sm text-text-body placeholder:text-text-muted focus:outline-none focus:ring-2 focus:ring-accent/30 focus:border-accent w-full
- Use `useQueryState('search', searchParamsParsers.search)` for state
- Debounce input: use a local state + useEffect with 300ms setTimeout to avoid updating URL on every keystroke. Or use nuqs `throttleMs` option: `useQueryState('search', searchParamsParsers.search.withOptions({ throttleMs: 300 }))`
- Placeholder text: "Search stays..."
- Show a clear X button when search has a value

2. Create `components/sort-toggle.tsx` — Client component:
- `'use client'`
- Use `useQueryState('sort', searchParamsParsers.sort)` for state
- Render as a button/link that cycles through: null → 'price-asc' → 'price-desc' → null
- Display text: when null: "↕ Sort" (neutral), when 'price-asc': "Price: low → high", when 'price-desc': "Price: high → low"
- Style: text-sm text-text-secondary hover:text-text-primary flex items-center gap-1 cursor-pointer. Use ArrowUpDown icon from lucide.

3. Update `app/(main)/page.tsx` to wire everything together:
- Import: SearchBar, SortToggle, StaysGrid, VibePicker (from 02-01), VibePickerMobile (from 02-01)
- Server component that reads search params and fetches stays
- Layout structure (matching Pencil design):
  ```
  <section className="py-6 space-y-6">
    {/* Toolbar row: vibe picker pill + search + sort */}
    <div className="flex items-center gap-3">
      <div className="hidden md:block"><VibePicker /></div>
      <div className="md:hidden"><VibePickerMobile /></div>
      <SearchBar />
      <SortToggle />
    </div>

    {/* Title row */}
    <div className="flex items-baseline justify-between">
      <h1 className="font-heading text-2xl font-semibold text-text-primary">
        Unique stays near you
      </h1>
      <span className="text-sm text-text-secondary">{stays.length} stays</span>
    </div>

    {/* Stays grid */}
    <StaysGrid stays={stays} />
  </section>
  ```
- Pass `searchParams` through nuqs `searchParamsCache.parse(searchParams)` to get type, vibe, search, sort
- Call `getStays({ type, vibe, search, sort })` and pass results to StaysGrid

4. Ensure the loading.tsx from plan 02-01 has proper skeleton grid that matches the final layout.
  </action>
  <verify>
    <automated>cd /Users/eduard/projects/03-demos/lateral-challenge && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Search bar filters stays by title/location with debounced input
    - Sort toggle cycles through price ascending and descending
    - Discovery page shows toolbar (vibe picker + search + sort), title with count, and stays grid
    - URL updates with ?search=treehouse or ?sort=price-asc params
    - Responsive grid displays 1/2/3 columns by viewport
    - Empty state appears when no stays match filters
    - Full build succeeds
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` completes without errors
2. Discovery page at `/` shows stay cards in a 3-column grid on desktop
3. Each card shows: Unsplash image, title, location, price/night (e.g., "$185 / night"), type badge, guest count
4. Resizing browser: 3 cols → 2 cols → 1 col with layout switching from vertical to horizontal cards on mobile
5. Typing in search bar filters stays (e.g., "cabin" shows only cabin stays)
6. Clicking sort toggle switches between price low→high and high→low
7. All filter params appear in URL (?type=solo&vibe=adventure&search=cabin&sort=price-asc)
8. Empty state shows when no stays match combined filters
</verification>

<success_criteria>
- Stay cards render with all required data fields matching Pencil design
- Grid is responsive: 1 col mobile, 2 col tablet, 3 col desktop
- Search filters by title and location
- Sort toggles price ordering
- All interactions update URL via nuqs
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-browsing-experience/02-02-SUMMARY.md`
</output>
